console.log("ts is working")
//max sum column 
function maxCol (matrix:number[][]):number{
    let maxNum = -Infinity;
    for(let i:number=0;i<matrix.length;i++){
        for(let i7u8)
    }


    return maxNum;
}